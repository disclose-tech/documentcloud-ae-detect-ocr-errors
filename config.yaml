title: AE Detect OCR Errors
description: Add-On to detect OCR errors in AE documents
type: object
documents: []
properties:
  project:
    title: Project ID
    type: integer
required:
  - project
categories: 
  - extraction
version: 2

eventOptions:
  events:
    - upload